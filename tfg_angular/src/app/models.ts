export interface Producto{
    nombre: string;
    descripcion: string;
    fotos: string;
    id: string;
    fecha_salida: Date;
}

export interface Usuario{
    uid: string;
    nombre: string;
    email: string;
    contrasena: string;

}